# FlashGen environment — copy to .env (same file the systemd unit and the
# flashgen-mcp container both read; docker-compose.yml loads it via env_file: ../.env).
#
#   cp .env.example .env   &&   edit values
#
# Bearer token Claude presents to the MCP server (OAuth token endpoint echoes it).
FLASHGEN_MCP_TOKEN=

# TTS / generation providers (set at least the one you use).
OPENAI_API_KEY=
GEMINI_API_KEY=

# AnkiWeb credentials are NOT set here. They are mounted into the anki container
# as Docker Compose file-secrets so they never land in the container env or
# `docker inspect`. Create them on the host (gitignored, deploy-rsync-excluded):
#   mkdir -p deploy/secrets
#   printf %s 'you@example.com' > deploy/secrets/ankiweb_username
#   printf %s 'your-password'   > deploy/secrets/ankiweb_password
#   chmod 600 deploy/secrets/ankiweb_*
# The flashgen-sync addon reads them via ANKIWEB_USERNAME_FILE / ANKIWEB_PASSWORD_FILE.

# NOTE: do NOT set ANKI_CONNECT_URL here. The flashgen-mcp container overrides it
# to http://anki:8765 in docker-compose.yml so it reaches AnkiConnect over the
# private compose network. The CLI defaults to http://127.0.0.1:8765.
